[package]
name = "reef-proto"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lints]
workspace = true

[dependencies]
serde        = { version = "1", features = ["derive"] }
serde_json   = { workspace = true }
